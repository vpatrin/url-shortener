# Copy to .env and update values
# Local dev: localhost (native uvicorn → Docker infra)
# Production: container names (everything in Docker)

# Database configuration
DB_HOST=shared-postgres        # local: localhost
DB_PORT=5432
DB_NAME=shortener
DB_USER=shortener
DB_PASSWORD=your-secure-password-here

# Redis configuration
REDIS_HOST=url-shortener-redis  # local: localhost
REDIS_PORT=6379
REDIS_DB=0

# Application configuration
BASE_URL=https://your-domain.com  # local: http://localhost:8000
DEFAULT_TTL_HOURS=24
CODE_LENGTH=6
